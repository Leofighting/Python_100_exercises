# 编写函数计算圆面积

# 从math模块中导入pi 并取名为 PI
from math import pi as PI


def circle_area(r):
    # 确保r 接收的是整形或者浮点数
    if isinstance(r, (int, float)):
        return PI * r * r
    else:
        print("You must give me a integer or float as radius")


area1 = circle_area(10)
print(round(area1, 2))


# 个人笔记：
# 可以用round()函数来保留小数点的后两位
