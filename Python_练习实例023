# 双色球：
# 每一注彩票由 6 个介于1到33之间的不重复数字
# 和 1 个介于1到16之间的数字组成。
import random


def double_color():
    red = random.sample(range(1, 34), 6)
    blue = random.choice(range(1, 17))
    return str(red) + " - " + str(blue)


print(double_color())


# 个人笔记：
# random.sample()
# 可以从指定的序列中，随机的截取指定长度的片断，不作原地修改
#
# random.choice()
# 可以从任何序列，比如list列表中，选取一个随机的元素返回，可以用于字符串、列表、元组等
